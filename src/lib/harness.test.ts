import { describe, expect, it } from 'vitest';

// Placeholder test proving the harness runs. The real seam —
// filterExercises(dataset, criteria) — arrives in ticket 02.
describe('test harness', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
